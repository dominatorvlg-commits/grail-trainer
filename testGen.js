import { generateBoard } from './src/utils/gameLogic.js'; const modes = ['verbs', 'nouns', 'irovanie', 'participles', 'adjectives']; async function testGeneration() { for (const mode of modes) { const start = Date.now(); console.log('Generating board for mode: ' + mode); try { const board = generateBoard(mode, 'easy'); const time = Date.now() - start; console.log('Success for ' + mode + ' in ' + time + 'ms'); } catch (e) { console.error('Error for ' + mode + ':', e); } } } testGeneration();
